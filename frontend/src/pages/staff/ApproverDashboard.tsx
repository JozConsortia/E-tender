import { Link } from 'react-router-dom'
import { useApp } from '../../context/AppContext'
import { EmptyState, PageHeader, StatCard, StatusBadge } from '../../components/Ui'
export default function ApproverDashboard() { const { tenders, applications } = useApp(); const pending = tenders.filter(t => t.status === 'APPROVAL'); const recs = applications.filter(a => pending.some(t => t.id === a.tenderId) && a.status === 'SHORTLISTED'); return <><PageHeader title="Final approval authority" description="Make the authorised award decision after evaluation and adjudication records are complete."/><div className="notice warning"><strong>Approval responsibility</strong><span>Confirm compliance, delegated authority, value-for-money considerations, and the completeness of the committee record before deciding.</span></div><div className="stats-grid"><StatCard label="Pending decisions" value={recs.length}/><StatCard label="Tenders in approval" value={pending.length}/><StatCard label="Awarded tenders" value={tenders.filter(t => t.status === 'AWARDED').length}/></div><div className="card table-card"><div className="card-head"><div><h3>Approval register</h3><p>Only adjudicated recommendations awaiting final authority appear here.</p></div></div>{recs.length ? <div className="table-wrap"><table><thead><tr><th>Reference</th><th>Recommended bidder</th><th>Score</th><th>Stage</th><th></th></tr></thead><tbody>{recs.map(a => <tr key={a.id}><td><strong>{a.tenderReference}</strong><small className="cell-title">{a.tenderTitle}</small></td><td>{a.companyName}</td><td>{a.finalScore ?? a.aiScore}/100</td><td><StatusBadge tone="warning">AWAITING APPROVAL</StatusBadge></td><td><Link className="text-link" to={`/approver/approvals/${a.id}`}>Review decision →</Link></td></tr>)}</tbody></table></div> : <EmptyState title="No decisions awaiting approval" message="BAC-approved recommendations will appear here for final decision." />}</div></> }
