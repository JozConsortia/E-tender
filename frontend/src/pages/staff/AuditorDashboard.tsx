import { useApp } from '../../context/AppContext'
import { PageHeader, StatCard } from '../../components/Ui'
export default function AuditorDashboard() { const { tenders, applications, auditLogs } = useApp(); return <><PageHeader title="Audit and oversight" description="Read-only visibility of procurement records, workflow actions, and accountability events."/><div className="notice info"><strong>Independent view</strong><span>Audit users cannot create, evaluate, adjudicate, approve, or alter procurement records.</span></div><div className="stats-grid"><StatCard label="Tender records" value={tenders.length}/><StatCard label="Applications" value={applications.length}/><StatCard label="Audit events" value={auditLogs.length}/><StatCard label="Awarded tenders" value={tenders.filter(t => t.status === 'AWARDED').length}/></div><div className="card table-card"><div className="card-head"><div><h3>Latest control activity</h3><p>Use the audit trail navigation for the complete chronological record.</p></div></div><div className="table-wrap"><table><thead><tr><th>Time</th><th>Actor</th><th>Action</th><th>Target</th></tr></thead><tbody>{auditLogs.slice(0, 10).map(l => <tr key={l.id}><td>{l.time}</td><td>{l.actor}</td><td>{l.action}</td><td>{l.target}</td></tr>)}</tbody></table></div></div></> }
