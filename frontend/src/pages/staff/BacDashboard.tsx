import { Link } from 'react-router-dom'
import { useApp } from '../../context/AppContext'
import { EmptyState, PageHeader, StatCard, StatusBadge } from '../../components/Ui'
export default function BacDashboard() { const { tenders, applications } = useApp(); const cases = tenders.filter(t => t.status === 'ADJUDICATION'); const recs = applications.filter(a => cases.some(t => t.id === a.tenderId) && a.status === 'SHORTLISTED'); return <><PageHeader title="Bid adjudication committee" description="Review the BEC record, test the recommendation, and refer an outcome for authorised approval."/><div className="notice info"><strong>Adjudication control</strong><span>The BAC considers the committee record and procurement compliance. It does not change the published criteria or perform the final approval.</span></div><div className="stats-grid"><StatCard label="Pending cases" value={cases.length}/><StatCard label="BEC recommendations" value={recs.length}/><StatCard label="Awaiting decision" value={recs.length}/></div><div className="card table-card"><div className="card-head"><div><h3>Adjudication register</h3><p>Cases below have completed BEC evaluation and are ready for committee review.</p></div></div>{recs.length ? <div className="table-wrap"><table><thead><tr><th>Reference</th><th>Recommended bidder</th><th>Score</th><th>Evidence</th><th></th></tr></thead><tbody>{recs.map(a => <tr key={a.id}><td><strong>{a.tenderReference}</strong><small className="cell-title">{a.tenderTitle}</small></td><td>{a.companyName}</td><td>{a.finalScore ?? a.aiScore}/100</td><td><StatusBadge tone="success">BEC COMPLETE</StatusBadge></td><td><Link className="text-link" to={`/bac/adjudication/${a.id}`}>Open case →</Link></td></tr>)}</tbody></table></div> : <EmptyState title="No cases awaiting adjudication" message="Completed BEC recommendations will appear here for committee decision." />}</div></> }
