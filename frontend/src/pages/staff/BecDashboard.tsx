import { Link } from 'react-router-dom'
import { useApp } from '../../context/AppContext'
import { EmptyState, PageHeader, StatCard, StatusBadge } from '../../components/Ui'
export default function BecDashboard() { const { applications, tenders } = useApp(); const evalTenders = tenders.filter(t => t.status === 'EVALUATION'); const assigned = applications.filter(a => evalTenders.some(t => t.id === a.tenderId)); return <><PageHeader title="Bid evaluation committee" description="Assess responsive bids against the published criteria and record a reasoned recommendation for adjudication."/><div className="notice info"><strong>Committee control</strong><span>AI analysis is advisory. Members must verify the evidence, apply the published criteria consistently, and preserve an auditable recommendation.</span></div><div className="stats-grid"><StatCard label="Open evaluations" value={evalTenders.length}/><StatCard label="Applications" value={assigned.length}/><StatCard label="AI assessments" value={assigned.filter(a => a.aiScore !== undefined).length}/><StatCard label="Awaiting review" value={assigned.filter(a => a.aiScore === undefined).length}/></div><div className="card table-card"><div className="card-head"><div><h3>Evaluation register</h3><p>Only tenders currently in evaluation are assigned to the BEC.</p></div></div>{assigned.length ? <div className="table-wrap"><table><thead><tr><th>Tender</th><th>Bidder</th><th>AI score</th><th>Recommendation</th><th></th></tr></thead><tbody>{assigned.map(a => <tr key={a.id}><td><strong>{a.tenderReference}</strong><small className="cell-title">{a.tenderTitle}</small></td><td>{a.companyName}</td><td>{a.aiScore ?? 'Pending'}</td><td><StatusBadge tone={a.aiRecommendation === 'QUALIFY' ? 'success' : 'warning'}>{a.aiRecommendation ?? 'PENDING'}</StatusBadge></td><td><Link className="text-link" to={`/bec/evaluations/${a.id}`}>Open evaluation →</Link></td></tr>)}</tbody></table></div> : <EmptyState title="No evaluations awaiting committee action" message="When an approved supplier submits a tender application, it will appear here for evidence review." />}</div></> }
