import { useParams } from 'react-router-dom'
import { useApp } from '../../context/AppContext'
import { PageHeader, StatusBadge } from '../../components/Ui'
export default function ApplicationDetails() { const { id } = useParams(); const { applications, currentUser } = useApp(); const a = applications.find(x => x.id === id && x.applicantId === currentUser?.id); if (!a) return <div className="empty-state"><h3>Application not found</h3><p>You can only access your own submissions.</p></div>; return <><PageHeader title="My application" description={`${a.tenderReference} · ${a.tenderTitle}`} /><div className="two-column"><div className="card"><h3>Submission status</h3><div className="result-card"><StatusBadge tone={a.status === 'SUCCESSFUL' ? 'success' : a.status === 'UNSUCCESSFUL' ? 'danger' : 'warning'}>{a.status.replace('_',' ')}</StatusBadge><p>Submitted {new Date(a.submittedAt).toLocaleString('en-ZA')}</p></div><h3>Submitted documents</h3><div className="file-list static">{a.documents.map(f => <span key={f}>{f}</span>)}</div></div><div className="card"><h3>Outcome</h3>{['SUCCESSFUL','UNSUCCESSFUL'].includes(a.status) ? <div className={`outcome-box ${a.status === 'SUCCESSFUL' ? 'success' : 'danger'}`}><strong>{a.status === 'SUCCESSFUL' ? 'Application successful' : 'Application unsuccessful'}</strong><p>The final procurement outcome has been recorded by the authorised workflow.</p></div> : <div className="notice info"><strong>Outcome pending</strong><span>Your application is still within the procurement workflow. Internal evaluation details are not available to applicants.</span></div>}</div></div></> }
