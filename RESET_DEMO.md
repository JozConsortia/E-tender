# Reset the demo

There is no database yet. The Node.js backend stores all data in memory.

To reset everything, stop and restart the backend:

```powershell
cd backend
npm run dev
```

Also sign out from the frontend if a previous JWT session is still active.
